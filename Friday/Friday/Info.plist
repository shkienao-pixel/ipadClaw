<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>

    <!-- ── UI keys (previously auto-generated by GENERATE_INFOPLIST_FILE = YES) ── -->

    <key>UIApplicationSceneManifest</key>
    <dict>
        <key>UIApplicationSupportsMultipleScenes</key>
        <false/>
        <key>UISceneConfigurations</key>
        <dict/>
    </dict>

    <key>UIApplicationSupportsIndirectInputEvents</key>
    <true/>

    <key>UILaunchScreen</key>
    <dict/>

    <key>UIStatusBarStyle</key>
    <string>UIStatusBarStyleDefault</string>

    <key>UISupportedInterfaceOrientations~ipad</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationPortraitUpsideDown</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>

    <key>UISupportedInterfaceOrientations~iphone</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>

    <!-- ── Permissions ── -->

    <key>NSMicrophoneUsageDescription</key>
    <string>Friday 需要麦克风权限以进行语音识别。</string>

    <key>NSSpeechRecognitionUsageDescription</key>
    <string>Friday 需要语音识别权限以将你的语音转为文字。</string>

    <!-- ── OpenClaw token ──────────────────────────────────────────────────────────
         SECURITY: Replace the placeholder below with your real token locally.
         Recommended: use OPENCLAW_TOKEN env var in Xcode scheme instead, and keep
         this file in .gitignore so the token is never committed to the repo.
         (Edit Scheme → Run → Arguments → Environment Variables → OPENCLAW_TOKEN)
    ─────────────────────────────────────────────────────────────────────────── -->
    <key>OpenClawToken</key>
    <string>REPLACE_WITH_YOUR_TOKEN</string>

    <!-- ── ATS: allow HTTP only for the specific OpenClaw gateway IP ─────────────
         NSIncludesSubdomains = false so only this exact host is excepted.
         Do NOT add NSAllowsArbitraryLoads — that disables ATS globally.
    ─────────────────────────────────────────────────────────────────────────── -->
    <key>NSAppTransportSecurity</key>
    <dict>
        <key>NSExceptionDomains</key>
        <dict>
            <key>100.118.204.105</key>
            <dict>
                <key>NSExceptionAllowsInsecureHTTPLoads</key>
                <true/>
                <key>NSIncludesSubdomains</key>
                <false/>
            </dict>
        </dict>
    </dict>

</dict>
</plist>
